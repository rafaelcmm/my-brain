# examples

Reference examples for MCP client setup and rule/skill examples.

## Files

- `mcp.example.json`: sample MCP client configuration
- `rules/self_learning.md`: sample operational rule text
- `skills/SKILL.md`: sample skill descriptor

## MCP Client Note

Example client expects `MCP_AUTH_TOKEN` in shell environment when using HTTP transport.
