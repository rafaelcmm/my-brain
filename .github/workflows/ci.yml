name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  required-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install root deps
        run: npm install
      - name: Root lint
        run: npm run lint
      - name: Root tests
        run: npm test
      - name: Prettier check
        run: npx prettier --check .

  lint-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/orchestrator
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Enable Corepack and pnpm
        run: |
          npm install -g corepack@0.31.0
          corepack enable
          corepack prepare pnpm@9.15.4 --activate

      - uses: actions/cache@v4
        with:
          path: ~/.local/share/pnpm/store
          key: pnpm-${{ runner.os }}-${{ hashFiles('src/orchestrator/pnpm-lock.yaml') }}
          restore-keys: pnpm-${{ runner.os }}-

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ludeeus/action-shellcheck@master
        with:
          scandir: ./src/scripts

  caddyfile-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Caddyfile
        run: |
          docker run --rm -v "$PWD/src/gateway:/etc/caddy" caddy:2-alpine caddy validate --config /etc/caddy/Caddyfile

  docker-build:
    runs-on: ubuntu-latest
    needs: [required-gates, lint-and-test]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build orchestrator image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: src/orchestrator/Dockerfile
          push: false
          tags: my-brain/orchestrator:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate docker-compose.yml
        run: |
          cp .env.example .env
          sed -i 's|MYBRAIN_DB_PASSWORD=.*|MYBRAIN_DB_PASSWORD=ci-test-password|' .env
          mkdir -p .secrets
          printf 'my-brain-ci-token' > .secrets/auth-token
          printf 'unused-placeholder' > .secrets/auth-token.previous
          chmod 700 .secrets
          chmod 600 .secrets/auth-token .secrets/auth-token.previous
          docker compose config > /dev/null

  prettier-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npx prettier --check .

  integration-smoke:
    runs-on: ubuntu-latest
    needs: [required-gates, compose-validate, caddyfile-validate, shellcheck]
    steps:
      - uses: actions/checkout@v4
      - name: Prepare env and secrets
        run: |
          cp .env.example .env
          sed -i 's|MYBRAIN_DB_PASSWORD=.*|MYBRAIN_DB_PASSWORD=ci-test-password|' .env
          mkdir -p .secrets
          printf 'my-brain-ci-token-abcdefghijklmnopqrstuvwxyz-ABCDEFGHIJKLMNOPQRSTUVWXYZ-1234' > .secrets/auth-token
          printf 'unused-placeholder' > .secrets/auth-token.previous
          chmod 700 .secrets
          chmod 600 .secrets/auth-token .secrets/auth-token.previous
      - name: Build and start stack
        run: docker compose up -d --build
      - name: Smoke test
        run: ./src/scripts/smoke-test.sh
      - name: Validate tool ids
        run: ./src/scripts/validate-tool-ids.sh
      - name: Dump logs on failure
        if: failure()
        run: docker compose logs --no-color
      - name: Tear down stack
        if: always()
        run: docker compose down -v
