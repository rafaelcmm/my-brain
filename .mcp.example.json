{
  "_comment": "Copy this file to .mcp.json (gitignored) and replace the token with the value from .secrets/auth-token. The MCP bridge is exposed on port 3333 through the Caddy gateway and only supports Streamable HTTP on /mcp.",
  "mcpServers": {
    "my-brain": {
      "type": "http",
      "url": "http://127.0.0.1:3333/mcp",
      "headers": {
        "Authorization": "Bearer my-brain-local-token"
      }
    }
  }
}
